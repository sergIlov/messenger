a.list-group-item.list-group-item-action href=conversation_messages_path(conversation)
  span.right.color-red = conversation.new_messages_count(current_user)
  = conversation.companion(current_user).email